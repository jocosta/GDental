@model IEnumerable<GDental.Core.Model.Tipo_Documento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoDocumento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdEmpresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoDocumento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FlgAtivo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoDocumento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdEmpresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDocumento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FlgAtivo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id = item.IdTipoDocumento }) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.IdTipoDocumento }) |
            @Html.ActionLink("Excluir", "Delete", new { id = item.IdTipoDocumento})
        </td>
    </tr>
}

</table>
