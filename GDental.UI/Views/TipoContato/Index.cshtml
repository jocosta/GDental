@model IEnumerable<GDental.Core.Model.Tipo_Contato>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTipoContato)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdEmpresa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoContato)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FlgAtivo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTipoContato)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdEmpresa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoContato)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FlgAtivo)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new {id=item.IdTipoContato }) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.IdTipoContato }) |
            @Html.ActionLink("Excluir", "Delete", new { id = item.IdTipoContato })
        </td>
    </tr>
}

</table>
